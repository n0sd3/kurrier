WEB_PORT=3000
WEB_URL=http://localhost:3000
NITRO_PORT=3001

# Hide Kurrier Drive and storage management from the web UI.
# Mail storage, attachments, and existing Drive data are not affected.
DISABLE_DRIVE=false

#LOCAL_TUNNEL_URL=https://your-ngrok-tunnel-url.ngrok-free.app

REDIS_PASSWORD=replace_with_your_redis_password
REDIS_HOST=redis
REDIS_PORT=6379

TYPESENSE_API_KEY=supersecretkey
TYPESENSE_PORT=8108
TYPESENSE_PROTOCOL=http
TYPESENSE_HOST=typesense

DOCS_PORT=3002
DOCS_URL=http://docs:3002

POSTGRES_USER=postgres
POSTGRES_PASSWORD=replace_with_your_password
POSTGRES_DB=postgres
POSTGRES_PORT=5432
POSTGRES_HOST=postgres

DATABASE_URL=postgresql://postgres:replace_with_your_password@postgres:5432/postgres
DATABASE_RLS_URL=postgresql://kurrier:replace_with_your_password@postgres:5432/postgres

JWT_SECRET=TpLjgE4eqXMeZLEclNH5bLuME53eQ6Rsl67M98Je
APP_SECRET_ENCRYPTION_KEY=s2Ef+V5lnMMcbh7NlLuIdxqbIp+cx2Q8Kec2eBGwrZk=
SEARCH_REBUILD_ON_BOOT=false

DAV_URL=http://localhost:5232

DAV_DATABASE_URL=postgresql://baikal:strong_baikal_password@baikal-postgres:5432/baikal
WORKER_URL=http://worker:3001
HOST=0.0.0.0


GARAGE_ACCESS_KEY=kurrierdev
GARAGE_SECRET_KEY=kurrierdevsupersecretkey
GARAGE_BUCKET=kurrier-store
GARAGE_RPC_SECRET=c067b927e0625a757baa4dd22172a5db4d8a0d4347944f6b5c9c7b3b2c406b91


S3_ENDPOINT=http://garage:3900
S3_ACCESS_KEY=kurrierdev
S3_SECRET_KEY=kurrierdevsupersecretkey
S3_BUCKET=kurrier-store
S3_REGION=garage
S3_FORCE_PATH_STYLE=true


OIDC_GOOGLE_CLIENT_ID=
OIDC_GOOGLE_CLIENT_SECRET=

# Generic OIDC provider (Authelia, Keycloak, Authentik, Zitadel, Okta, ...)
# All three are required to enable the generic SSO login button.
OIDC_ISSUER_URL=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
# Optional: label shown on the login button (defaults to "SSO")
OIDC_PROVIDER_NAME=
# Optional: requested scopes (defaults to "openid email profile")
OIDC_SCOPES=
# Optional: token endpoint auth method, client_secret_basic (default) or client_secret_post
OIDC_TOKEN_AUTH_METHOD=
# Optional: set to "false" to accept unverified emails from IdPs that never
# set email_verified to true (defaults to "true", i.e. required)
OIDC_REQUIRE_VERIFIED_EMAIL=

# Instance-level admin API key (worker): lets infrastructure automation act
# on behalf of any user (pre-provision users, SMTP accounts, identities).
# Opt-in; 32+ chars required. Generate with: openssl rand -hex 32
API_ADMIN_KEY=

# Web push notifications (web + worker): enables browser push for new inbox
# email. Opt-in; if any of these are unset, the feature silently no-ops.
# Generate a keypair with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=

# Optional instance-wide SMTP/IMAP presets shown to every user.
# Credentials must not be included here; users enter their own credentials in Kurrier.
CUSTOM_EMAIL_PROVIDERS=[]
